from event import *

timestep = 0

def accelerate(Train, station): # function to decide train acceleration
    if Train.position < station[0] :
        

def simulation():
    accelerate()
    event()
    timestep += 1
    if timestep >= 1000 :
        printsituation() ##


def printsituation(Train, ESS):
    print("Train position is in ")
